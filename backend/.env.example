PORT=5000
NODE_ENV=development
JWT_SECRET=smartmart_super_secure_jwt_secret_key_2026_production
CLIENT_URL=http://localhost:5173

# MySQL Configuration (Optional - system falls back to SQLite automatically if MySQL is unreachable)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=smartmart_db

# Redis Configuration (Optional - falls back to high-performance in-memory cache if Redis is unreachable)
REDIS_URL=redis://localhost:6379

# AI/ML Configuration
AI_CONFIDENCE_THRESHOLD=0.85
